% =====================================================================
%  Abbreviations.tex — Danh mục từ viết tắt
%
%  File này CỐ Ý để trống. Danh sách 22 từ viết tắt thật của đồ án đã được
%  viết trực tiếp trong main.tex (ngay sau \input{pages/Abbreviations}).
%  Việc tách riêng file này là để tương thích với cấu trúc template,
%  nhưng KHÔNG thêm từ mới nào ở đây — tránh trùng lặp.
%
%  Nếu muốn gộp toàn bộ danh sách vào đây thay vì nhúng vào main.tex:
%    1. Cắt khối \begin{tabular}{ll}...\end{tabular} trong main.tex (sau khi tách)
%    2. Paste vào file này, đặt sau lệnh \label{abbrev}
%    3. Xoá khối đó khỏi main.tex
% =====================================================================

% File trống — nội dung danh sách đặt ở main.tex.
